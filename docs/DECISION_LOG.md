# DECISION_LOG.md — Riwayat Keputusan Teknis & Bisnis

> Setiap keputusan penting dicatat di sini: **ID, tanggal, keputusan, alasan, dampak, status**. Jangan ubah keputusan yang sudah terkunci tanpa persetujuan pemilik — buat entri baru yang men-supersede bila perlu.
>
> Terakhir diperbarui: 2026-07-14

| ID | Tgl | Keputusan | Alasan | Dampak / Terkait | Status |
|---|---|---|---|---|---|
| **D1** | 2026-07-03 | Ketersediaan/stok **tidak diotomatiskan** di MVP; customer ajukan booking → admin konfirmasi manual. Stok qty + ledger barang masuk/keluar + checklist gudang = **Phase 2**. | Belum ada data jumlah unit fisik; gap diisi subrental; jaga MVP tetap cepat rilis. | Data model disiapkan agar stok bisa nyambung nanti (DG4). | ✅ Terkunci |
| **D2** | 2026-07-03 | Katalog MVP mencakup semua lini (barang sewa, habis pakai, catering, kru). | Semua sudah bagian dari order existing. | Model produk harus dukung tipe heterogen (DG2). | ✅ Terkunci |
| **D3** | 2026-07-03 | **Sewa Lokasi ditunda ke Phase 2** (product type "location", struktur mirror item). | Rencana lokasi belum matang. | Data model dibuat extensible. | ✅ Terkunci |
| **D4** | 2026-07-03 | **Overtime** dicatat via **kategori denda** (Rp100k/jam barang, Rp50k/jam kru). | Overtime = tagihan setelah fakta pada order yang ada → cocok pola invoice `-D`. | Entity penalty punya kategori. | ✅ Terkunci |
| **D5** | 2026-07-03 | **Deposit/jaminan AKTIF di MVP.** | Sudah ada di sheet & bagian dari nilai bayar. | Field deposit% + deposit amount + grand total+deposit. | ✅ Terkunci |
| **D6** | 2026-07-03 | Default **Status Pembayaran = "Belum Lunas"** (koreksi dari default "Lunas" di sheet). | Cegah salah anggap sudah bayar. | Perubahan perilaku dari existing — disetujui pemilik. | ✅ Terkunci |
| **D7** | 2026-07-03 | Nomor order **tetap `DR-DDMMYY-NNNN`** dengan counter **global** (tidak reset). | Kontinuitas dengan kode yang sudah berjalan. | Backend pakai sequence/counter, format dipertahankan (KG3). | ✅ Terkunci |
| **D8** | 2026-07-03 | **Invoice otomatis ditunda ke Phase 2.** Backend = sumber kebenaran & menyajikan data order siap-invoice; selama dev invoice **ditarik/di-render manual**; **backend tidak menulis balik ke Sheets.** | Reuse aset invoice existing tidak masuk jalur kritis MVP; hindari dual source of truth. | Data model & API wajib menyimpan/menyajikan semua field invoice (header + item + total). | ✅ Terkunci |
| **D9** | 2026-07-03 | **Produk = satu tabel `products` + kolom `type`** (rental/expendable/catering/crew/location), bukan tabel terpisah per lini. (R1) | Skala kecil, sistem kode sudah seragam; hindari overengineering. | Perilaku harga dibedakan `type`/`pricing_basis`. | ✅ Terkunci |
| **D10** | 2026-07-03 | **Bundle = 1 baris `order_item`** (harga bundle) di MVP; pecah komponen ditunda ke Phase 2 (saat stok). (R4) | Sesuai perilaku sheet sekarang; komponen baru perlu saat tracking stok. | order_item punya `catalog_type` product/bundle. | ✅ Terkunci |
| **D11** | 2026-07-03 | **Customer = guest** (isi nama+telp, admin konfirmasi) di MVP; disimpan di tabel `customers` tanpa auth; staff di `users`. Akun/login = Future. (Q1, refine R2) | Paling simpel, cocok D1; alur konfirmasi manual. | Cek status customer = kode order + phone. | ✅ Terkunci |
| **D12** | 2026-07-03 | **`products.code` wajib, unik global, & tidak boleh direuse**; produk pensiun = **soft delete** (`is_active=false`). (Q5) | Kode dipakai tracking barang keluar; reuse kode = rusak jejak. | Tidak ada hard delete produk. | ✅ Terkunci |
| **D13** | 2026-07-03 | **Diskon hanya per-baris item** (bukan level order); persen atau nominal. (Q2, resolve O2) | Tidak semua item didiskon. | Field diskon ada di `order_items`. | ✅ Terkunci |
| **D14** | 2026-07-03 | **Satu denda per order** (kode `-D`); dihitung setelah periode maksimal selesai. (Q4, R6) | Denda final di akhir periode. | penalties 1:1 order. | ✅ Terkunci |
| **D15** | 2026-07-03 | **Pembatalan: disposisi DP fleksibel** — refund penuh / hangus / sebagian; admin tentukan; refund tercatat di ledger `payments`. (Q6, resolve O1) | Bisa jadi kesalahan pihak DuRent. | `orders.dp_disposition` + payment kind `refund`. | ✅ Terkunci |
| **D16** | 2026-07-03 | **Arsitektur:** modular monolith · layered (API→Service→Repository) · relational DB · **pricing engine** & **code generator** service khusus · integrasi via adapter · uang = integer rupiah · RBAC (customer-guest/gudang/admin/owner). | Skala kecil tapi harus rapi & scalable; hindari overengineering (microservices). | Semua modul mengikuti pola ini. **Tech stack vendor spesifik ditunda.** | ✅ Terkunci |
| **D17** | 2026-07-03 | **Efisiensi FE↔BE:** REST + list-ringkas/detail-agregat · `?include=` · cache katalog (ETag/Cache-Control) · pagination/filter server-side · gzip · eager-load anti-N+1 · `POST /orders/preview` untuk harga live. **GraphQL/tRPC ditolak** (overengineering). | Minim round-trip & over/under-fetch tanpa menambah kompleksitas. | API_CONTRACT §11; `GET /orders/{code}` jadi agregat. | ✅ Terkunci |
| **D18** | 2026-07-03 | **Model cutover migrasi = Opsi A (master-only + fresh start):** hanya master data dipindah; transaksi historis tetap arsip read-only di spreadsheet; order counter lanjut dari `MAX(NNNN)+1`. Migrasi historis = Phase 2 (opsional). | Volume historis kecil; MVP order lahir di backend; risiko rendah. | MIGRATION_PLAN.md. | ✅ Terkunci |
| **D19** | 2026-07-03 | **`POST /orders/preview` diambil untuk MVP** (harga live tanpa simpan); **`GET /dashboard/summary` ditunda ke Future** (bareng reporting). | Preview menjaga kalkulasi 1 sumber + UX booking; dashboard summary ikut penundaan reporting. | API_CONTRACT §5.0/§5; FRONTEND_PREPARATION §5. | ✅ Terkunci |
| **D20** | 2026-07-03 | **Tech stack: NestJS + Prisma (TypeScript) + PostgreSQL**; hosting free-tier (app: Render/Railway; DB: Neon/Supabase); Swagger (API docs), Jest (test), class-validator, @nestjs/throttler. | Modular by-design cocok arsitektur; TS manfaatkan familiaritas JS (Apps Script) + full-stack bila FE React/Next; gratis-dulu & scalable. | Semua modul/kode mengikuti. TECH_STACK.md; BACKEND_ARCHITECTURE §15. | ✅ Terkunci |
| **D21** | 2026-07-03 | **Konvensi scaffold (Tahap 0):** NestJS 11 + Prisma 7 (generator `prisma-client`, output `src/generated/prisma`, `moduleFormat=cjs`, driver adapter `@prisma/adapter-pg`, config via `prisma.config.ts`) · logger **nestjs-pino** (JSON; pino-pretty saat dev; redact header Authorization) · global prefix **`/api/v1`** · Swagger UI di **`/api/docs`** · ValidationPipe global (whitelist+transform, gagal = **422**) · exception filter global bentuk `{error:{code,message,details}}` dengan kode baku (VALIDATION_FAILED, NOT_FOUND, CONFLICT, dst.) · ThrottlerGuard global 60 req/menit (publik diperketat per-endpoint nanti) · koneksi DB **lazy** (DATABASE_URL placeholder sampai Tahap 1) · `postinstall: prisma generate`. | Prisma 7 tak lagi men-generate ke node_modules & butuh adapter; output di `src/` menjaga layout `dist/main.js`; sisanya implementasi langsung dari API_CONTRACT §1 + BACKEND_ARCHITECTURE §9–10. | Struktur `src/` per TECH_STACK §5; 8 modul domain kosong siap diisi Tahap 1–5; health check `GET /api/v1/health`. | ✅ Terkunci |
| **D22** | 2026-07-03 | **Konvensi & hasil Tahap 1 (Catalog):** ① uang = `Int` rupiah, field Prisma **snake_case** persis DATA_MODEL (respons API tanpa lapisan mapping); ② kode produk `DS-{UNIV}-{CAT}-{SUB}-{NNNN}` via `ProductCodeService` (MAX+1 per kombinasi; soft delete TIDAK membebaskan nomor); ③ **kode bundle `DS-BI-{CAT}-{NNNN}`** — segmen `BI` konstan (sheet: "Bundling Items"), nomor per category utama → `bundles` + kolom `category_utama_code`, `code_number`; ④ kolom *Harga* sheet Bundling Code = **harga bundle**; `component_price` = snapshot `base_price` komponen; `original_price` (derived) = Σ(qty×component_price); ⑤ type `location` ditolak di API (D3); ⑥ import master idempotent dari sheet asli: **100 produk, 8 bundle/24 komponen, 36 voucher**; kode diambil verbatim (kode kru 4-segmen `DS-CW-…` dipertahankan), 1 perbaikan bug sheet: kode kembar `DS-CW-OT-DC` → `-0001`/`-0002`; field turunan diisi aturan MIGRATION §4 (perlu review pemilik); kode voucher dipertahankan case/spasi aslinya (lookup nanti sebaiknya case-insensitive — dibahas Tahap 3); ⑦ endpoint admin belum ber-guard sampai Tahap 2 (dev-only, disetujui). | Format kode dikonfirmasi dari sheet "Database Inventory" asli; hindari parsing string untuk nomor urut; kontinuitas kode existing (D7/D12). | prisma/import-master.ts · import-vouchers.ts · DATA_MODEL §3.4. | ✅ Terkunci |
| **D23** | 2026-07-06 | **Struktur repo = monorepo folder:** `backend/` (NestJS+Prisma — semua kode server), `frontend/` (placeholder, diisi fase FE), `docs/` (bersama), `tools/` (alat dev: `api-tester.html` — 13 smoke test browser hijau/merah terhadap API). Semua perintah npm dijalankan dari `backend/`. FE tetap satu repo (bukan repo terpisah). | Pemisahan lokasi perbaikan FE vs BE jelas untuk pemilik; satu repo cukup untuk tim kecil; deploy nanti tinggal set root directory `backend/` di Render/Railway. Riwayat git dipertahankan (rename). | README root; semua path CI/deploy ke depan relatif `backend/`. | ✅ Terkunci |
| **D24** | 2026-07-06 | **Auth Tahap 2:** JWT **stateless** — logout = client membuang token (tanpa blacklist server, cukup untuk skala DuRent); `@nestjs/passport`+`passport-jwt`+`argon2` sesuai TECH_STACK; strategi validasi **cek DB tiap request** (user dinonaktifkan langsung tertolak, tanpa menunggu token kedaluwarsa); login di-throttle **5x/menit**; expiry via `JWT_EXPIRES_IN` (default 1d); endpoint admin katalog dikunci `admin`+`owner`; akun staff dikelola via **seed** (CRUD user API tidak dibuat — tidak ada di API_CONTRACT §3); catatan teknis: Jest perlu flag `--experimental-vm-modules` karena Prisma 7 memakai dynamic import saat query. | Keamanan cukup tanpa overengineering; kontrak API diikuti apa adanya. | Semua endpoint 🔵 selanjutnya (orders/payments/penalties) tinggal pasang guard yang sama. | ✅ Terkunci |
| **D25** | 2026-07-14 | **Konvensi & hasil Tahap 3 (Orders + Pricing Engine):** ① Pricing Engine = fungsi murni (`pricing-engine.ts`), rumus persis sheet (durasi inklusif +1; amount; rental; diskon per-baris persen/nominal; deposit % dari Σ rental PRA-diskon); hasil persen pecahan dibulatkan **Math.round** (sheet menyimpan desimal → selisih maks Rp1 — **perlu review pemilik**); diskon nominal tidak di-clamp (paritas sheet); ② checkbox diskon TANPA kode promo = tanpa diskon (paritas XLOOKUP kosong), bukan error; voucher lookup **case-insensitive + trim** (tindak lanjut D22 ⑥); ③ kode order: DDMMYY dari zona **WIB (UTC+7)**; NNNN = MAX+1 global via kolom baru `orders.code_number` (unique; pola D22 ③ — tanpa parsing string; retry bila balapan); ④ kolom teknis baru `order_items.line_no` = urutan baris invoice stabil (kebutuhan D8; sheet menjaga urutan fisik baris); ⑤ enum `status_pembayaran` = belum_lunas/**sebagian**/lunas (3 nilai sesuai FRONTEND_PREPARATION §4; derived dari ledger di Tahap 4); ⑥ customer guest didedup per **phone persis**; nama+phone tetap di-snapshot di order (R3); ⑦ `PATCH /orders/{code}`: `items` yang dikirim MENGGANTI seluruh baris (harga = katalog saat ini); tanpa `items`, snapshot harga lama dipertahankan; `code`+`invoice_date` tetap; ⑧ `GET /orders/{code}` sudah balas bentuk agregat penuh — `billing` dihitung, `payments[]`/`penalties[]` kosong sampai Tahap 4–5 (kontrak FE stabil); ⑨ confirm idempoten; aksi pada order cancel → 409; refund saat cancel dicatat via modul payments (Tahap 4, D15); ⑩ throttle publik: create 5×/menit · lookup 10×/menit · preview 30×/menit; lookup menormalkan phone (08… ≡ 628…) dan balas 404 tunggal (tak bocorkan kode valid); ⑪ `ADMIN_WA_NUMBER` env → `whatsapp_admin_url` (null bila kosong). | Paritas formula = KG2/MG2; kontinuitas kode = D7/D18; efisiensi FE = D17. | `backend/src/modules/orders/*`, `integrations/whatsapp.adapter.ts`; migrasi `orders_order_items` (diterapkan ke Supabase 2026-07-14, e2e 12/12 hijau); DATA_MODEL §3.5–3.6. | ✅ Diimplementasi — poin ① pembulatan & ⑥ dedup phone perlu review pemilik |
| **D26** | 2026-07-14 | **Konvensi & hasil Tahap 4 (Payments):** ① semua baris ledger disimpan **positif** — refund DIKURANGKAN saat derivasi (DATA_MODEL "negatif/terpisah" → dipilih *terpisah*, lebih aman dari salah tanda); ② derivasi status (fungsi murni `payment-status.ts`, dipakai payments & orders): dibayar ≤ 0 → `belum_lunas` (D6) · ≥ total_tagihan → `lunas` (paritas aturan sheet finance) · sisanya → `sebagian`; di-persist ke `orders.status_pembayaran` dalam transaksi yang sama tiap mutasi payment **dan** tiap `PATCH /orders` (total berubah); ③ order **cancel hanya menerima refund** (dp/pelunasan → 409) — jalur disposisi DP D15; ④ kelebihan bayar diizinkan (`outstanding` boleh negatif — informasi FE); ⑤ kolom teknis `payments.created_at` untuk urutan ledger stabil; ⑥ `total_tagihan` = `total_with_deposit` (+ Σ denda menyusul Tahap 5); ⑦ respons `POST /payments` = `{payment, billing}` agar FE tak perlu refetch (D17). | Ledger per-order sesuai R5/BUSINESS_FLOW §6; koreksi default "Lunas" sheet (D6). | `backend/src/modules/payments/*`; migrasi `payments` (diterapkan); detail agregat `GET /orders/{code}` kini berisi ledger nyata. | ✅ Diimplementasi |
| **D27** | 2026-07-14 | **Konvensi & hasil Tahap 5 (Penalties):** ① `penalties.status_transaksi`/`status_pembayaran` **TIDAK disimpan** (beda dari DATA_MODEL awal yang menyebut kolom ini) — di-**derive dari order induk** saat respons, karena keduanya cuma lookup di sheet asli (CURRENT_SPREADSHEET_STRUCTURE §4.2: "Status Transaksi/Status Pembayaran ← lookup") dan menyimpan salinan berisiko tak sinkron; ② `total_tagihan` (payments/billing & detail agregat order) kini **total_with_deposit + Σ penalties.grand_total**, melengkapi rumus D26 ⑥; menambah denda bisa menurunkan order dari `lunas` ke `sebagian`, dihitung ulang di transaksi yang sama; ③ satu denda per order ditegakkan lewat `@unique` di `penalties.order_id` + cek eksplisit → 409 CONFLICT saat percobaan kedua (D14); ④ order **cancel menolak denda baru** (409) — selaras aturan payments (D26 ③); ⑤ `product_name`/`product_code` di `penalty_items` adalah **input bebas** (bukan FK ke `products`), persis sheet Database Denda — barang yang sudah dipensiunkan/berubah kode tetap bisa dicatat apa adanya; ⑥ kolom teknis `penalty_items.line_no` (pola D25 ④); ⑦ `GET /orders/{code}/penalties` & respons detail agregat membalas **array 0/1 elemen** (bukan objek nullable) — konsisten dengan konvensi list kosong FRONTEND_PREPARATION §3. | Paritas sheet Database Denda; hindari duplikasi status yang bisa divergen (lesson dari D6 soal default Lunas yang salah). | `backend/src/modules/penalties/*`; migrasi `penalties` (diterapkan); DATA_MODEL §3.8 perlu ditandai kolom status sebagai derived, bukan disimpan. | ✅ Diimplementasi — poin ① (skema menyimpang dari DATA_MODEL awal) perlu review pemilik |

---

## Keputusan yang Masih Terbuka

| ID | Pertanyaan | Status | Diperlukan untuk |
|---|---|---|---|
| ~~O1~~ | Aturan pembatalan (DP) | ✅ Terjawab → **D15** | — |
| ~~O2~~ | Cara diskon diterapkan di website | ✅ Terjawab → **D13** (per-baris) | — |
| **O3** | Detail **sewa lokasi** (pengkodean, atribut) | ⏳ Terbuka | Phase 2 |
